# 04-示例代码

本页只记录仓库内真实存在、可由 `examples/CMakeLists.txt` 生成 target 的示例，不再使用与源码不一致的 CamelCase 别名。本页同时取代旧的 `examples/README.md`，作为唯一示例索引入口。

## 构建前提

```bash
cmake -S . -B build \
  -DCMAKE_BUILD_TYPE=Release \
  -DGALAY_MYSQL_BUILD_EXAMPLES=ON
cmake --build build --parallel
```

import 示例需要额外满足模块工具链条件：

下面给的是可直接复制的 `Ninja` 版本；如果你使用的是 Visual Studio 生成器，请仅替换 `-G` 参数。

```bash
cmake -S . -B build-import -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DGALAY_MYSQL_BUILD_EXAMPLES=ON \
  -DGALAY_MYSQL_ENABLE_IMPORT_COMPILATION=ON \
  -DGALAY_MYSQL_BUILD_MODULE_EXAMPLES=ON
cmake --build build-import --parallel
```

## 运行前环境变量

示例公共配置来自 `examples/common/config.h`，优先读取：

- `GALAY_MYSQL_HOST`
- `GALAY_MYSQL_PORT`
- `GALAY_MYSQL_USER`
- `GALAY_MYSQL_PASSWORD`
- `GALAY_MYSQL_DB`

兼容回退变量：

- `MYSQL_HOST`
- `MYSQL_PORT`
- `MYSQL_USER`
- `MYSQL_PASSWORD`
- `MYSQL_DATABASE`

## include 示例映射

| 示例 | 真实源码文件 | 真实 target | 运行命令 | 用途 |
| --- | --- | --- | --- | --- |
| e1 | `examples/include/e1_query.cc` | `e1_query-Include` | `./build/examples/e1_query-Include` | 异步连接与单条查询 |
| e2 | `examples/include/e2_query.cc` | `e2_query-Include` | `./build/examples/e2_query-Include` | 同步连接与查询 |
| e3 | `examples/include/e3_pool.cc` | `e3_pool-Include` | `./build/examples/e3_pool-Include` | 异步连接池 acquire / release |
| e4 | `examples/include/e4_prepared.cc` | `e4_prepared-Include` | `./build/examples/e4_prepared-Include` | 同步事务 + 预处理语句 |
| e5 | `examples/include/e5_pipeline.cc` | `e5_pipeline-Include` | `./build/examples/e5_pipeline-Include` | 异步 pipeline |

## import 示例映射

| 示例 | 真实源码文件 | 真实 target | 运行命令 | 用途 |
| --- | --- | --- | --- | --- |
| e1 | `examples/import/e1_query.cc` | `e1_query-Import` | `./build-import/examples/e1_query-Import` | 模块导入版异步连接与查询 |
| e2 | `examples/import/e2_query.cc` | `e2_query-Import` | `./build-import/examples/e2_query-Import` | 模块导入版同步连接与查询 |
| e3 | `examples/import/e3_pool.cc` | `e3_pool-Import` | `./build-import/examples/e3_pool-Import` | 模块导入版异步连接池 |
| e4 | `examples/import/e4_prepared.cc` | `e4_prepared-Import` | `./build-import/examples/e4_prepared-Import` | 模块导入版同步事务 + 预处理语句 |
| e5 | `examples/import/e5_pipeline.cc` | `e5_pipeline-Import` | `./build-import/examples/e5_pipeline-Import` | 模块导入版异步 pipeline |

## 每个示例验证的能力

### `e1_query`

- 创建 `Runtime` / `IOScheduler`
- 使用 `AsyncMysqlClientBuilder` 构造客户端
- `co_await client.connect(...)`
- `co_await client.query("SELECT 1")`
- `co_await client.close()`

### `e2_query`

- 使用 `MysqlConfig::create(...)`
- `MysqlClient::connect()`
- `MysqlClient::query()`
- 遍历 `MysqlResultSet`

### `e3_pool`

- 配置 `MysqlConnectionPoolConfig`
- `co_await pool.acquire()`
- 对 `AsyncMysqlClient*` 执行查询
- `pool.release(client)`

### `e4_prepared`

- `beginTransaction()`
- `prepare()`
- `stmtExecute()`
- `stmtClose()`
- `commit()`

### `e5_pipeline`

- 构造 `std::vector<std::string_view>`
- `co_await client.pipeline(...)`
- 校验返回结果个数与内容

## 推荐验证顺序

1. 先跑 `e2_query-Include`，确认数据库连通性
2. 再跑 `e1_query-Include` 和 `e3_pool-Include`
3. 需要事务与预处理时跑 `e4_prepared-Include`
4. 需要串联查询时跑 `e5_pipeline-Include`
5. import / module 版本只在工具链确认可用后再跑

## 与测试页的关系

- `examples/` 面向接入示范
- `test/` 面向行为验证
- `benchmark/` 面向压测，不应混入示例页
